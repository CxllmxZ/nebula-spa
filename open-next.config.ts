/**
 * OpenNext Cloudflare configuration
 *
 * Transforms Next.js build output for Cloudflare Workers runtime.
 * @see https://opennext.js.org/cloudflare
 *
 * Used by: pnpm exec opennextjs-cloudflare build
 * (Runs after `next build`, produces .open-next/ output)
 */

//import { defineCloudflareConfig } from "@opennextjs/cloudflare";

/*export default defineCloudflareConfig({
  // TODO: Configure incremental cache (KV or R2) when needed
  // For MVP: default (no cache) is fine
  //
  // Example (add later):
  // import r2IncrementalCache from "@opennextjs/cloudflare/overrides/incremental-cache/r2-incremental-cache";
  // incrementalCache: r2IncrementalCache,
});*/

export {};
